import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        primary: {
            main: 'rgb(36,41,47)',
        },
        secondary: {
            main: 'rgb(9,105,218)',
        },
        background: {
            main: 'rgb(246,248,250)',
        }
    },
});